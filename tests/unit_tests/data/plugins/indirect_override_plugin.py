# A plugin file that defines no microservice of its own, and instead only imports the
# module that defines the overriding microservices.
from indirect_override_defs import indirect_override_me  # noqa: F401
