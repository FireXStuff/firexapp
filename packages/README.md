# Transitional workspace members

This directory contains the independently buildable distributions that are
temporarily co-located in the `firexapp` uv workspace during the OSS monorepo
migration.

`workspace-smoke` is a non-publishing scaffold used to validate the workspace
CI before the first real component is imported. Remove it when a real member
provides the same selected-member coverage.
