# FireX workspace smoke member

This package is a temporary, non-publishing workspace member. It proves that a
member can be locked, synchronized, linted, tested, and built independently of
the root `firexapp` distribution.
